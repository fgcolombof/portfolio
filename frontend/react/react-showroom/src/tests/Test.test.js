import { describe } from "vitest";

describe("Counter component test suite", () => {
  it("should vitest library configured correctly", () => {
    console.log("Successfully vitest configured ....");
  });
});
