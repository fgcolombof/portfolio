[build]
  publish = "dist" # O "build", según si usás Vite o CRA

[dev]
  command = "npm run dev:vite"
  port = 8888
  targetPort = 5173

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200